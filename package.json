{
  "name": "local-pair-review",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.3"
  },
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "bun run --filter @local-pair-review/shared build && bun run --filter @local-pair-review/server build && bun run --filter @local-pair-review/web build",
    "dev": "bun run --parallel --filter './apps/*' dev",
    "test": "bun test",
    "test:e2e": "playwright test",
    "smoke:codex": "bun run scripts/smoke.ts codex",
    "smoke:claude": "bun run scripts/smoke.ts claude",
    "typecheck": "tsc --noEmit -p packages/shared && tsc --noEmit -p apps/server && tsc --noEmit -p apps/web"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
